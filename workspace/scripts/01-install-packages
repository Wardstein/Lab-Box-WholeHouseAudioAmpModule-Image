set -x
set -e

export LC_ALL=C

source /common.sh
install_cleanup_trap

# Scripts run as root by default


# Install pyside2 and qml packages
apt update
apt install -y alsa-utils wget libvorbisidec1 git
